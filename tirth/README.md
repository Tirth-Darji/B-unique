# Member 2 — Tirth

This folder is owned by Tirth for assigned B-unique React work.

## Assigned work

- Add assigned pages, components, styles, assets, and tests here.
- Record important setup notes and dependencies in this file.
- Follow the shared theme and component conventions.
- Push work to `main` as required by the hackathon.
- Pull the latest `main` before editing and again before pushing.

## Push workflow

```bash
git switch main
git pull origin main
git add tirth/
git commit -m "feat: describe your change"
git pull --rebase origin main
git push origin main
```

## Suggested structure

```text
tirth/
├── components/
├── pages/
├── assets/
├── styles/
└── README.md
```
