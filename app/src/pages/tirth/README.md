# Tirth's pages

Create Tirth's assigned React page components in this folder.

Example:

```text
DashboardPage.jsx
CreateTripPage.jsx
TripListPage.jsx
```

Tirth is also the default integrator for shared routing, global styles, dependencies, and final builds.
