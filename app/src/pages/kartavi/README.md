# Kartavi's pages

Create Kartavi's assigned React page components in this folder.

Example:

```text
LoginPage.jsx
SignupPage.jsx
ProfilePage.jsx
```

Export one React component per file. Ask the integrator to connect completed pages to the application routes.
